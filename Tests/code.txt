fn sum(numlist as vector, numb as int) <int> {
    <% This is a comment
     asds
       %>
    i :: int = 0;
    j :: int = 0;
    x :: boolean = false;
    print("sda")
    scan(x)
    if [[ n < 2 ]]
     begin
        return
     end
    fn sum(a as int, b as int) <int> return a+b;
    length(n)
    list(n)
    exit(n)
    result :: int = 0;
    for (i = 0 to length(numlist))
        begin
            result = result + numlist[i];
        end
    if x < 0
        begin
            return 0;
        end
    elseif x = 0
        begin
            return result;
        end
    else
        begin
            return result;
        end

    return result;
}